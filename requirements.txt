# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Configuration
pydantic-settings>=2.7.0
python-dotenv>=1.0.0

# HTTP client (async)
httpx>=0.27.0

# Error tracking
sentry-sdk[fastapi]>=2.22.0

# ── Added in Phase 2 (TTS) ──────────────────────────────
gtts>=2.5.0          # fallback TTS (Coqui XTTS-v2 runs in Docker, called over HTTP)

# ── Added in Phase 3 (STT) ──────────────────────────────
python-multipart>=0.0.9   # file uploads for POST /api/stt/transcribe
# Deepgram STT is called via httpx REST — no SDK needed.
# The self-hosted Whisper engine (faster-whisper) is an OPT-IN install kept in a
# separate file so it can't break this one on newer Pythons:
#     pip install -r requirements-stt.txt

# ── Added in Phase 4 (LLM brain) ────────────────────────
anthropic>=0.40.0    # Claude API (the reasoning brain)

# ── Added in Phase 5 (Training / tiered response) ───────

# ── Testing ─────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-mock>=3.14.0
httpx>=0.27.0
